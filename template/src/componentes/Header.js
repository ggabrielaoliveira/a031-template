import React from "react";
import Logo from "../img/logo.png"

export default function Header(){
    return(
        <header>
          <h1>garagem gabi</h1>
          <img src={Logo}/>  
        </header>
    )
}